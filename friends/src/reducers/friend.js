/*import {

} from "../actions";*/

const initialState = {
  friends: []
};

export const friendReducer = (state = initialState, action) => {
  switch (action.type) {

    default:
      return state;
  }
};
