/*import {

} from "../actions";*/

const initialState = {
  friends: [],
  err: null
};

export const loginReducer = (state = initialState, action) => {
  switch (action.type) {

    default:
      return state;
  }
};
